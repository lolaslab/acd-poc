# Accessibility Compat Data Collector

## /lib
### /lib/constructor.rb

### /lib/fetch_runs.rb

### /lib/search.rb

### lib/stream_html_aam_results.rb

## /bin
### /bin/init.rb
- Using the JAWS example data create a list of json files in data/html/elements
### /bin/run.rb
- Run the constructor to generate the browser json test files based off of WPT runs.